# Lab 1 — Process Creation (C++17, CMake)

This project implements three console applications:

- **creator** — creates a binary file with employee records
- **reporter** — generates a text report based on the binary file
- **main_app** — orchestrates both utilities using Windows processes


